#!/usr/local/bin/sharpl

(if (not (= #ARG 3)) 
  (say "Usage: client [Host] [Port] [Password]")
  (stop))

(var HOST (ARG 0)
     PORT (parse-int:_ (ARG 1))
     PASSWORD (ARG 2))

(let [c (net/connect HOST:PORT)]
  (say c)
  (close c))