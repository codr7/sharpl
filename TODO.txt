* add Label/Goto ops
** use reference for label
** add Sharpl.Label type with pc
** track labels in VM
* add CallDirect op
** eval like indirect with target
** override Method EmitCall like Prim
* add id reader
* add dec macro
** add macros
* add call form
* add call reader
* add Int reader

* add op ToString()
** emit lisp

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))

* add stack type
** add stack reader