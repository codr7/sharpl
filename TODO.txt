* add Loc
* add forms
** add id form
* add readers
** add id reader
* add methods
* add call op
* add stack type
* add macros
* add int reader
* add stack reader

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))