* add call form
** claes
* add call reader

* add dec macro
** add macros
* add Int reader
* add Bit type

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))

* add stack type
** add stack reader