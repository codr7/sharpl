* add id reader
* add decr macro
** add macros
* add call form
* add call reader
* add literal form
* add int reader

* add op ToString()
** emit lisp

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))

* add stack type
** add stack reader