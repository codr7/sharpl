* add id reader
* add decr macro
** add macros
* add Op.ToString()
** emit lisp
* add Literal form
* add int reader

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))

* add stack type
** add stack reader