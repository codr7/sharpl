* fix error

core

* handle errors in repl
* import core into user

Int

* add dec macro
** add macros
* add call form
* add call reader
* add Int reader
* add Bit type

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))

* add stack type
** add stack reader