* add Lib print support

* add repl

* add forms
* add readers
* add methods
* add call op
* add stack type
* add macros
* add int reader
* add stack reader

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))