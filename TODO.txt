* add Bit type
* add Int reader
* add dec macro
** add macros

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))

* add support for lib names

(term/read-line)

* add echo flag to read-key

* add stack type
** add stack reader

* add rgb method
** return ansi escapes to set color

* add read-line method

(^ repl []
  (print "sharpl v" VERSION " - may the src be with you\n\n")

  (^ read-input [buffer]
    (print "  ")
    (let [line (read-line)]
      (if line
        (rec (push buffer line)
        (string/join buffer \n)))))
  
  (let [input (read-input)]
    (if input
      (do 
        (eval input)
        (repl)))))