* add specialized EmitArgs type
** use LinkedList for items
* add Op.ToString()
** emit lisp
* add Literal form
* add readers
** add id reader
** add int reader
* add methods
* add call op
* add stack type
* add stack reader
* add macros

fib

(^ fib-rec [n]
  (if (< n 2) n (+ (fib-rec (decr n)) (fib-rec (decr n)))))